
package ${包名}.service;

import ${实体类全称};
import java.util.Map;

public interface ${实体类简称}Service {

    boolean add${实体类简称}(${实体类简称} ${实体类对象名});

    boolean delete${实体类简称}(${实体类简称} ${实体类对象名});

    boolean update${实体类简称}(${实体类简称} ${实体类对象名});

    Map<String, Object> query${实体类简称}(${实体类简称} ${实体类对象名});
}
