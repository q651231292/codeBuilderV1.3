
package ${包名}.dao.impl;

import com.rgy.test.dao.${实体类简称}Dao;
import ${实体类全称};
import java.util.HashMap;
import java.util.Map;

public class ${实体类简称}DaoImpl implements ${实体类简称}Dao {

    Map<String, Object> result ;
    
    @Override
    public boolean add${实体类简称}(${实体类简称} ${实体类对象名}) {

        return true;
    }

    @Override
    public boolean delete${实体类简称}(${实体类简称} ${实体类对象名}) {

        return true;
    }

    @Override
    public boolean update${实体类简称}(${实体类简称} ${实体类对象名}) {

        return true;
    }

    @Override
    public Map<String, Object> query${实体类简称}(${实体类简称} ${实体类对象名}) {
        result = new HashMap<>();

        return result;
    }

}
